<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.fbansept.demo.android.pocsynchro">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BlocNote"
        android:usesCleartextTraffic="true">
        <activity android:name="edu.fbansept.demo.android.pocsynchro.view.ListeIncidentActivity" />
        <activity android:name="edu.fbansept.demo.android.pocsynchro.view.EditionIncidentActivity" />
        <activity android:name="edu.fbansept.demo.android.pocsynchro.view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>